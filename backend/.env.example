# Copia este archivo como .env y define tus propios valores.
# NUNCA subas el archivo .env real al repositorio (ver .gitignore).
PORT=4000
FRONTEND_ORIGIN=http://localhost:5173
CMS_API_KEY=cambia-esta-clave-por-una-propia
